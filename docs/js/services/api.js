// Funções genéricas de fetch (GET, POST) reutilizáveis//

import { API_URL } from '../config.js';
// Pega a URL base do backend definida centralmente, em vez de escrever ela espalhada em vários arquivos