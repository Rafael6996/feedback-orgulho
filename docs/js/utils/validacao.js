//  Funções de validação extra no front (além do required do HTML) //

import { NOTA_MINIMA, NOTA_MAXIMA } from '../config.js';
// Usa os limites definidos centralmente, em vez de "chumbar" os números 1 e 5 direto aqui