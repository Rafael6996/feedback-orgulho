// Função que monta o gráfico do Chart.js (reutilizável) // 

// Chart.js é carregado via <script> no HTML (CDN), não via import,
// então ele já fica disponível globalmente como "Chart" dentro deste arquivo